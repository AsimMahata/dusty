import React from 'react';

export const PinDot: React.FC = () => {
    return (
        <span className="pin-dot" title="Pinned" style={{ display: 'inline-block', verticalAlign: 'middle', marginBottom: '2px' }} />
    );
};