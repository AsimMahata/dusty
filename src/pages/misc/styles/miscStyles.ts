import React from 'react';

export const getComingSoonStyle = (): React.CSSProperties => ({
    display: 'flex',
    height: '40vh',
    alignItems: 'center',
    justifyContent: 'center',
    color: 'var(--text-muted)'
});
